export const siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? "https://belgobase.com";

/** Set to true to show the coming soon page instead of the full landing. */
export const comingSoonEnabled = false;
